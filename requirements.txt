# EEEM068 Applied Machine Learning
# MSCTD Multimodal Sentiment Analysis — Python Dependencies
#
# All packages below are installed automatically in Cell 1 of the notebook.
# For manual installation: pip install -r requirements.txt
#
# Tested on: Google Colab (Python 3.10, CUDA 12.x, T4 GPU)

# ── Core Deep Learning ──────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Computer Vision ─────────────────────────────────────────────
facenet-pytorch>=2.5.3       # MTCNN face detector + ResNet face encoder
albumentations>=1.3.0        # Image augmentation (used by facenet-pytorch)
opencv-python>=4.7.0         # cv2 fallback for malformed JPEG loading
Pillow>=10.1.0               # PIL image loading (must be >=10.1 for scikit-image compat)

# ── Transformers (Extra Credit — MultimodalTransformer cell) ────
transformers>=4.35.0         # ViTModel, BertModel, BertTokenizer
datasets>=2.14.0             # HuggingFace datasets utility

# ── Data & Visualisation ────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0          # classification_report, confusion_matrix, roc_curve

# ── Utilities ───────────────────────────────────────────────────
tqdm>=4.65.0                 # progress bars during training/evaluation
gdown>=4.7.0                 # Google Drive download (must be >=4.3.1; --id flag removed)
